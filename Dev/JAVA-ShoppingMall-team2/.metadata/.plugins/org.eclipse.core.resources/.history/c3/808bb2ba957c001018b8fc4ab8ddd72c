package com.shopping.test;

import java.util.List;

import com.shopping.model.User;
import com.shopping.persistence.FileManager;

public class File_NameTest {
	List<User> data = FileManager.readFromFile(FILE_NAME);
	for (Object obj : data) {
	    System.out.println("저장된 객체 타입: " + obj.getClass().getName());
	}
	


